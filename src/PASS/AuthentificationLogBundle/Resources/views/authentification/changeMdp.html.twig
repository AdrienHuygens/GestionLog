{% extends "::base.html.twig" %}

{# empty Twig template #}
{% block contenu %}
<form action="{{ path('PASS_ChangeMDP', {'personneId':id}) }}" {{ form_enctype(form) }} method="POST">
{{ form_widget(form) }}
<div>
<input type="submit" value="{{ 'change_password.submit'|trans }}" />
</div>
</form>

  {% endblock %}