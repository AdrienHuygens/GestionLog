{# empty Twig template #}
{% if is_granted("ROLE_USER_R") or is_granted("ROLE_ADMIN") %}
<a href="{{ path('PASS_GestionUtilisateur',{'listingId': jeune.id}) }}"><span class="glyphicon glyphicon-user" title="Voir l'utilisateur"  aria-hidden="true"></span> </a>
   {% endif %}    

{% if is_granted("ROLE_LDAP_I") or is_granted("ROLE_ADMIN") %}
<a href="{{ path('PASS_modificationLDAP',{'username': jeune.username}) }}"><span class="glyphicon glyphicon-refresh" title="Modifier l'utilisateur"  aria-hidden="true"></span> </a>
  {% endif %}
 {% if is_granted("ROLE_USER_D") or is_granted("ROLE_ADMIN") %}                           
<a href="{{ path('PASS_SupprimerUtilisateur',{'personneId': jeune.id}) }}">
    <span class="glyphicon glyphicon-trash" title="Supprimer l'utilisateur"  aria-hidden="true"></span> </a>
   {% endif %}
    
    {% if is_granted("ROLE_DROIT_U") or is_granted("ROLE_ADMIN") %}
<a href="{{ path('PASS_droitGroupeUtilisateur',{'personne': jeune.id}) }}">
    <span class="glyphicon glyphicon-eye-open" title="Changer les groupe et droit de l'utilisateur"  aria-hidden="true"></span> </a>
 
 {% endif %}
 